
/* The plus symbol has has double duty it's serving to be the addition operator 
but it's also serving to concatenate two values together in this case
to string values together so that we can print it out the screen. */

let x = 8;
let y = 2;
let z = x + y;
console.log('Answer: ' + z);

